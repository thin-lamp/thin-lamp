import { ToggleWakeLockCommand } from './toggle-wake-lock-command';

describe('ToggleWakeLockCommand', () => {
  it('should create an instance', () => {
    expect(new ToggleWakeLockCommand()).toBeTruthy();
  });
});
